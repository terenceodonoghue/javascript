import { css } from '@emotion/css';

export const content = {
  center: css({
    alignContent: 'center',
  }),
  end: css({
    alignContent: 'end',
  }),
  start: css({
    alignContent: 'start',
  }),
};

export const items = {
  center: css({
    alignItems: 'center',
  }),
  end: css({
    alignItems: 'end',
  }),
  start: css({
    alignItems: 'start',
  }),
};
