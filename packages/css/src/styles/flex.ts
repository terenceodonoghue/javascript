import { css } from '@emotion/css';

export const direction = {
  column: css({
    flexDirection: 'column',
  }),
  row: css({
    flexDirection: 'row',
  }),
};

export const grow = {
  1: css({ flexGrow: 1 }),
};
