import { css } from '@emotion/css';

export const content = {
  center: css({
    justifyContent: 'center',
  }),
  end: css({
    justifyContent: 'end',
  }),
  start: css({
    justifyContent: 'start',
  }),
};

export const items = {
  center: css({
    justifyItems: 'center',
  }),
  end: css({
    justifyItems: 'end',
  }),
  start: css({
    justifyItems: 'start',
  }),
};
