import { css } from '@emotion/css';

export const listStyleType = {
  none: css({
    listStyleType: 'none',
  }),
};

export const textAlign = {
  center: css({
    textAlign: 'center',
  }),
  left: css({
    textAlign: 'left',
  }),
  right: css({
    textAlign: 'right',
  }),
};
