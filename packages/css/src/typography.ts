import { css } from '@emotion/css';

/**
 * Text alignment
 */

export const textAlign = {
  center: css({
    textAlign: 'center',
  }),
  left: css({
    textAlign: 'left',
  }),
  right: css({
    textAlign: 'right',
  }),
};
